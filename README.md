# MEXC Telegram Bot

Telegram бот для получения информации о криптовалютных парах с бирж MEXC и Gate.io.

## Особенности

- Получение информации о фьючерсных контрактах
- Отображение спотовых цен
- Агрегация данных с нескольких бирж
- Поддержка команд в группах
- MarkdownV2 форматирование
- **Мониторинг fair price алертов через WebSocket**

## Команды

- `/start` - Приветственное сообщение
- `/mexc <symbol>` - Информация с MEXC (например: `/mexc BTC_USDT`)
- `/gate <symbol>` - Информация с Gate.io (например: `/gate BTC_USDT`)
- `/cex <symbol>` - Агрегированная информация с обеих бирж (например: `/cex BTC`)

### Пример ответа на `/mexc BTC_USDT`:

```
🔔 *BTC_USDT* | Status 🟢

📈 *Spread:* +2.45% 🔴 SHORT
💰 *Last:* $43,250.50 | *Fair:* $42,200.30 | *Index:* $42,180.75
📊 *Volume 24h:* 1,500,000 BTC | 65,000,000,000 USDT

*Buy Limit:* $25,000

*Index:* Binance 40.0% • OKX 30.0% • Huobi 20.0% • Other 10.0%

*ETHEREUM:* D: ✅ | W: ✅
`0x2260FAC5E5542a773Aa44fBCfeDf7C193bc2C599`
[DexScreener](https://dexscreener.com/ethereum/0x2260fac5e5542a773aa44fbcfed7c193bc2c599) | [GMGN](https://gmgn.ai/eth/token/0x2260fac5e5542a773aa44fbcfed7c193bc2c599)

🔗 [Trade](https://futures.mexc.com/exchange/BTC_USDT)
```

## Fair Price Мониторинг

Бот включает систему мониторинга fair price через WebSocket API MEXC. При обнаружении значительного расхождения между последней ценой и справедливой ценой (>5%) отправляется алерт в указанный канал.

### Формат алерта (Markdown)

```
⚠️ *Fair Price Alert* | 🔴 SHORT

🏦 *MEXC*

📊 *Ticker:* [1/USDT](https://www.mexc.com/ru-RU/futures/1/USDT)
📋 *Copy:* `1`
💰 *Last Price:* `0.01528`
🎯 *Fair Price:* `0.01421`
📈 *Spread:* `+7.53%`
📊 *Volume 24h:* `11,370,149`
🏛️ *Index Pool:* MEXC (Binance 40.0% • OKX 30.0% • Huobi 20.0%)  *(может показывать "MEXC" если API недоступно)*
🌐 *DEX Networks:* [Ethereum](https://dexscreener.com/ethereum/0x...) • [GMGN](https://gmgn.ai/eth/token/0x...)
💰 *Buy Limit:* `$1,250.00` *(API Required / $0.00 / Position Only)*

🕐 `2026-01-20 23:59:38` | TTL: 1h
```

**Особенности:**
- **MarkdownV2 форматирование** с жирным текстом и моноширинным шрифтом
- **TTL (Time To Live)** - повторные алерты по тому же символу блокируются на 1 час
- **Индексация монеты** - показывает состав индекса с весами бирж (Binance, OKX, Huobi и др.)
- **DEX сети** - показывает доступные сети для монеты со ссылками на DexScreener
- **Buy Limit** - показывает максимальную сумму в $ для покупки токенов на основе баланса и контрактных лимитов (требуется MEXC API ключ)
- **Полное экранирование** всех специальных символов MarkdownV2 (*[]()~`>#+-=|{}!.%-)

### Запуск мониторинга

**Мониторинг автоматически запускается при запуске основного бота** (`python main.py` или `docker-compose up`).

#### Дополнительные варианты запуска

##### Через Docker Compose (отдельный контейнер)
```bash
docker-compose up fair-price-monitor
```

##### Через cron (Linux/Mac)
```bash
# Сделать скрипт исполняемым
chmod +x cron-fair-price-monitor

# Добавить в crontab (каждые 5 минут)
crontab -e
*/5 * * * * /path/to/project/cron-fair-price-monitor
```

##### Ручной запуск скрипта
```bash
python fair_price_monitor.py
```

## Запуск

### Локально

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` на основе `env.example`:
```bash
cp env.example .env
```

3. Заполните переменные окружения в `.env`

4. Запустите бота:
```bash
python main.py
```

### Docker

1. Создайте файл `.env` на основе `env.example`

2. Соберите и запустите через Docker Compose:
```bash
docker-compose up --build
```

Или в фоне:
```bash
docker-compose up -d --build
```

### Остановка

```bash
docker-compose down
```

## Переменные окружения

- `BOT_TOKEN` - Токен Telegram бота
- `ALERT_CHAT_ID` - ID канала для алертов (по умолчанию: -1003321617744)
- `MEXC_API_KEY` - API ключ MEXC (требуется для Buy Limit)
- `MEXC_API_SECRET` - API секрет MEXC (требуется для Buy Limit)
- `GATE_API_KEY` - API ключ Gate.io
- `GATE_API_SECRET` - API секрет Gate.io

## Архитектура

Проект использует чистую архитектуру (Clean Architecture):

- `domain/` - бизнес-логика
- `application/` - сервисы приложения
- `infrastructure/` - внешние зависимости (API клиенты, HTTP клиент)
- `core/` - конфигурация, логирование, утилиты
- `bot/` - обработчики команд Telegram

## Логирование

Логи сохраняются в папку `logs/` при запуске через Docker.

## Настройка Fair Price Мониторинга

**Мониторинг автоматически запускается вместе с основным ботом.**

1. **Переменная окружения**: Установите `ALERT_CHAT_ID=-1003321617744` в `.env`

2. **Запуск с ботом**:
```bash
# Основной запуск (включает мониторинг)
python main.py

# Или через Docker
docker-compose up -d
```

3. **Дополнительные варианты запуска** (если нужен отдельный процесс):

   - **Docker отдельный контейнер**:
   ```bash
   docker-compose up fair-price-monitor
   ```

   - **Cron** (Linux/Mac):
   ```bash
   chmod +x cron-fair-price-monitor
   crontab -e
   # Добавить: */5 * * * * /path/to/project/cron-fair-price-monitor
   ```

   - **Ручной скрипт**:
   ```bash
   python fair_price_monitor.py
   ```

## Архитектура Мониторинга

- `MexcWebSocketClient` - WebSocket клиент для MEXC с автоматическим переподключением
- `FairPriceAlertService` - Сервис алертов с продвинутой логикой мониторинга
- `NetworkPrefixUtils` - Утилита для определения префиксов сетей DexScreener/GMGN
- **TTL система (Time To Live)** - кулдаун алертов 1 час на символ для предотвращения спама
- **MarkdownV2 форматирование** - красивые алерты с жирным текстом и ссылками
- **Индексация монет** - получение состава индексов с весами бирж (MEXC API)
- **DEX сети** - информация о доступных сетях для монет со ссылками на DexScreener (MEXC API)
- **Buy Limit** - расчет максимальной суммы в $ для покупки токенов на основе account assets и contract details (MEXC API, требуется API ключ)
- **Мониторинг спреда >5%** между lastPrice и fairPrice (настраивается)
- **Автоматическое экранирование** специальных символов для Telegram
